# Runtime dependencies
boto3>=1.43.32

# System dependency (not installable via pip):
# - AWS CLI v2 must be installed and configured with SSO
#   See: https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html

# Python version requirement: Python 3.9+ (for list[str] type annotations)

# Development dependencies (optional)
# Install with: pip install flake8 black pre-commit

# Code quality and formatting
# flake8>=6.0.0
# black>=23.0.0
# pre-commit>=3.0.0
